# vigill357.github.io
# 无限理想乡 · 创作宇宙

> 一个用于展示原创小说、衍生故事与世界观设定的静态网站，部署于 GitHub Pages。

---

## 项目简介

本仓库是 **「无限理想乡」创作宇宙** 的官方展示站点，包含主线小说目录、衍生故事卡片、插画图鉴与世界观数据库四大板块。网站为纯静态前端（HTML + CSS + JS），无需后端，所有内容数据通过本地 JSON / txt 文件驱动，在 GitHub Pages 上开箱即用。

---

## 文件结构

```
vigill357.github.io/
├── index.html                   # 主页面（网站入口）
├── novel.html                   # 小说目录页（完整章节列表）
├── reader.html                  # 章节阅读页
├── css/
│   └── main.css                 # 全局样式表（13 个模块）
├── js/
│   ├── app.js                   # 主入口脚本
│   ├── cursor.js                # 几何光标模块
│   ├── loader.js                # 内容加载与数据同步模块
│   └── markup.js                # 自定义标记语言解析器
└── data/
    ├── world.json               # 全站元数据（小说信息、衍生故事、插画、世界观）
    ├── main_stories/
    │   ├── index.json           # 章节文件夹列表
    │   ├── ch1/
    │   │   ├── ch1.txt          # 第一章正文（使用 Markup 语法）
    │   │   └── meta.json        # 第一章元数据
    │   └── ch2/
    │       ├── ch2.txt
    │       └── meta.json
    ├── side_stories/
    │   └── index.json           # 衍生故事列表（预留）
    └── world/
        └── index.json           # 世界观数据（预留）
```

---

## 各文件详细说明

### `index.html` — 网站主页面

网站的主 HTML 入口，承载所有板块的结构与布局：

| 区域 | 说明 |
|------|------|
| 几何光标 | 由两个 SVG 组成的三脚架造型自定义光标，随鼠标移动 |
| 导航栏 `<nav>` | 固定顶部，包含站名、锚点链接、连载状态指示灯 |
| Hero 首屏 | 全屏封面，含标题、简介、快速入口按钮、实时统计信息面板 |
| §01 主线小说 | 展示主线长篇信息与最新章节列表（由 loader.js 动态渲染） |
| §02 衍生故事 | 三列卡片式布局，展示各衍生短篇 |
| §03 插画图鉴 | 四列网格，展示角色/场景/概念插画占位框 |
| §04 世界观与设定 | 人物、地理、时间线、规则四个入口卡片 |
| 页脚 `<footer>` | 版权信息与最近更新日期 |

按依赖顺序引入三个 JS 脚本：`cursor.js` → `loader.js` → `app.js`。

---

### `novel.html` — 小说目录页

独立的完整章节列表页面，展示小说简介、统计数据与全部章节卡片。相较主页侧栏，本页提供更完整的章节信息（封面插图、小节副标题、所属弧线等）。通过面包屑导航与主页互联。

---

### `reader.html` — 章节阅读页

章节阅读界面，主要功能：

- **正文渲染**：调用 `Markup.parse()` 将 `.txt` 原始文本转换为带样式的 HTML，支持全部自定义标记语法（见下文 markup.js 说明）。
- **章节导航**：页面顶部与底部均设有上/下章快速跳转。
- **固定导航栏**：包含品牌标识与功能按钮（返回目录等）。
- **排版优化**：阅读区域宽度受限、行高宽松，适合长文阅读。

---

### `css/main.css` — 全局样式表

共 13 个模块，统一管理全站视觉风格：

| 模块 | 内容 |
|------|------|
| 模块 1 · 全局颜色变量 | CSS 自定义属性（`--bg0` 至 `--text3`）定义深蓝色系配色方案 |
| 模块 2 · 全局基础样式 | Reset、平滑滚动、网格纹理（`body::before`）、扫描线纹理（`body::after`）、自定义滚动条 |
| 模块 3 · 通用动画关键帧 | `fadeUp`、`fadeLeft`、`fadeIn`、`blink`、`breathe`、`scrollDown` |
| 模块 4 · 几何光标 | 内部三脚架（即时跟随）与外部三角环（延迟跟随）；悬停时旋转 60° + 放大 1.3 倍（`.big` 类） |
| 模块 5 · 点击波纹 | `.rpl` 类，点击时从鼠标位置向外扩散的圆形波纹动画 |
| 模块 6 · 导航栏 | 固定顶部毛玻璃导航；链接下划线滑入动画；状态灯闪烁效果 |
| 模块 7 · 首屏 Hero | 全屏居中布局；三个发光气泡（`.orb`）呼吸动画；按钮多边形裁切（`clip-path`）；信息面板入场动画 |
| 模块 8 · 内容板块通用 | 所有 `<section>` 的统一内边距；节头（标签 + 分割线 + 编号）样式 |
| 模块 9 · 小说板块 | 左主右侧的两栏网格；章节列表条目箭头滑动效果 |
| 模块 10 · 衍生故事板块 | 三列等宽卡片网格；卡片 hover 背景变化 |
| 模块 11 · 插画图鉴板块 | 四列 2:3 纵向卡片；图框 hover 微缩放效果 |
| 模块 12 · 世界观板块 | 两列两行世界观入口卡片；数量统计区块样式 |
| 模块 13 · 页脚 | 三栏水平分布布局；半透明深色背景 |

---

### `js/cursor.js` — 几何光标模块

封装为 IIFE 模块 `Cursor`，对外暴露 `init()` 方法：

- **即时跟随**：监听 `mousemove`，内部三脚架（`#cur`）直接跟随鼠标坐标。
- **延迟跟随**：通过 `requestAnimationFrame` 循环，以 0.15 的插值系数让外部三角环（`#cur-ring`）平滑滞后跟随，产生惯性拖尾效果。
- **悬停放大**：对 `a`、`button`、`.story-card` 等可交互元素切换 `.big` 类，触发旋转放大动画。
- **点击波纹**：监听全局 `click` 事件，在点击坐标创建 `.rpl` 元素，0.65 秒后自动移除。

---

### `js/loader.js` — 内容加载与数据同步模块

封装为 IIFE 模块 `Loader`，对外暴露 `init()` 方法。负责从 `data/` 目录异步拉取数据并渲染到页面：

| 核心功能 | 说明 |
|----------|------|
| 读取章节索引 | 拉取 `data/main_stories/index.json`，获取所有章节文件夹名列表 |
| 加载章节元数据 | 对每个文件夹异步拉取 `meta.json`，获取标题、副标题、日期、所属弧线等信息 |
| 累计字数统计 | 读取各章节 `.txt` 正文，统计字符数并汇总显示在小说统计栏 |
| 渲染章节列表 | 将章节条目动态插入主页侧栏及小说目录页 |
| 同步统计面板 | 将章节总数、故事数、插画数、最近更新日期填入 Hero 信息面板 |
| 渲染衍生故事 | 读取 `world.json` 中的 `stories` 字段，渲染三列故事卡片 |
| 渲染世界观统计 | 读取 `world.json` 中的 `world` 字段，将各项数量填入世界观卡片 |

所有网络请求均包含 `try/catch`，失败时静默降级，保持 HTML 中的占位内容不变。

---

### `js/app.js` — 主入口脚本

在 `DOMContentLoaded` 事件后执行：

1. **导航平滑滚动**：拦截所有 `href` 以 `#` 开头的锚点链接，改用 `scrollIntoView({ behavior: 'smooth' })` 平滑滚动。
2. **模块初始化调度**：依序调用 `Cursor.init()` 与 `Loader.init()`，启动光标系统与数据加载。

---

### `js/markup.js` — 自定义标记语言解析器

封装为 IIFE 模块 `Markup`，对外暴露 `parse(rawText)` 方法，将带标记的纯文本转换为 HTML 字符串，供 `reader.html` 渲染章节正文。

样式通过 `<style id="mk-styles">` 动态注入 `<head>`，仅注入一次。所有文本在输出前经 `esc()` 函数转义（`&`、`<`、`>`、`"` → HTML 实体），防止注入攻击。

#### A · 行内样式（嵌入段落中）

| 语法 | 效果 | CSS 类 |
|------|------|--------|
| `**文字**` | 金色强调（加粗） | `.mk-em` |
| `*文字*` | 内心独白（斜体淡蓝） | `.mk-thought` |
| `{{文字}}` | 数据/变量（等宽青色背景） | `.mk-var` |

> 解析顺序：`**` 先于 `*` 先于 `{{}}`，顺序不可颠倒。

#### B · 整段块（独占一行）

| 语法 | 效果 | CSS 类 |
|------|------|--------|
| `[[文字]]` | 居中蓝字（关键台词 / 信号） | `.mk-center` |
| `> 文字` | 系统日志（等宽青色左边框） | `.mk-log` |
| `<< 文字 >>` | 通讯频道（等宽绿色左边框） | `.mk-comm` |
| `((文字))` | 叙事旁白（右对齐灰色斜体） | `.mk-aside` |
| `!![[文字]]!!` | 系统警告（红色等宽方框） | `.mk-warn` |
| `^^^ 文字 ^^^` | 广播公告（金边居中） | `.mk-broadcast` |
| `\|\|\| 文字 \|\|\|` | 档案引用块（边框容器） | `.mk-archive` |
| `!!! 文字 !!!` | 危险叙事块（红色左边框） | `.mk-danger` |

#### C · 结构标记

| 语法 | 效果 | CSS 类 |
|------|------|--------|
| `---` | 场景切换（菱形分隔线） | `.mk-break` |
| `=== 文字 ===` | 章内小节标题（带分割线） | `.mk-section` |
| `### 文字` | 时间戳 / 地点标注（等宽小字） | `.mk-timestamp` |
| 空行 | 段落间距（1 行 = 普通间距，≥2 行 = 加大间距） | `.mk-gap` / `.mk-gap-lg` |

#### 排版规格

| 属性 | 值 |
|------|----|
| 正文字体 | Noto Serif SC，17px，行高 2.15 |
| 等宽字体 | Share Tech Mono |
| 正文颜色 | `#C0D4E8`（浅蓝灰） |
| 金色强调 | `#C8A84E` |
| 青色数据 | `#A8E6F5` |
| 系统日志 | `#5BC8E8` |
| 通讯频道 | `#8BE8A0` |
| 系统警告 | `#E06060` |

#### 示例

```
### 2187-03-14 · 深渊基地 β 区

主控台的显示屏闪烁了一下，*她感觉到某种不对劲*。

> SYSTEM: 连接请求已接入 — ID: UNKNOWN

[[「你在哪里？」]]

**警报**突然响起。

!![[WARNING: 核心过载 — 剩余时间 00:03:22]]!!

---

<< 频道 7-ALPHA: 我听到你了。 >>

((她没有回答。))
```

---

### `data/world.json` — 全站元数据

网站的核心数据文件，结构如下：

```jsonc
{
  "novel": {
    "title":    "《主线小说标题》",
    "synopsis": "小说简介文字",
    "status":   "连载中"
  },
  "stories": [          // 衍生故事列表，驱动三列卡片
    {
      "title":    "故事名",
      "status":   "完结 | 连载",
      "words":    "字数（或 null）",
      "chapters": "章数（或 null）",
      "excerpt":  "简短描述",
      "href":     "外链（可选）"
    }
  ],
  "gallery": [          // 插画元数据，驱动四列图鉴
    { "id": "char-01", "type": "character | scene | concept", "label": "标题" }
  ],
  "world": {            // 世界观数量统计，填入四张卡片
    "characters":    0,
    "locations":     0,
    "timelineNodes": 0,
    "rules":         0
  }
}
```

---

### `data/main_stories/` — 章节数据目录

每章独立存放于以章节编号命名的文件夹中：

```
main_stories/
├── index.json          # 文件夹名列表，如 ["ch1", "ch2"]
└── ch1/
    ├── meta.json       # 章节元数据
    └── ch1.txt         # 章节正文（使用 Markup 语法）
```

**`meta.json` 字段说明：**

```jsonc
{
  "title":        "序· 第一章",       // 章节标题
  "sub_title":    "「副标题」",        // 章节副标题
  "date":         "2026-04-01",       // 更新日期
  "synopsis":     "章节摘要",          // 简短描述
  "arc":          "序章",             // 所属弧线名
  "illus":        "illus.jpg",        // 封面插图文件名（同目录下）
  "illusCaption": "图注文字",          // 插图说明
  "illusTag":     "标签1 · 标签2"     // 插图标签
}
```

**`ch1.txt` 格式：** 使用 Markup 语法的纯文本，由 `Markup.parse()` 在阅读器中渲染。

---

## 快速上手：更新内容

| 操作 | 做法 |
|------|------|
| 新增一章 | 在 `data/main_stories/` 下创建新文件夹（如 `ch3/`），添加 `ch3.txt` 和 `meta.json`，并在 `index.json` 数组中追加 `"ch3"` |
| 修改小说信息 | 编辑 `world.json` 中的 `novel.title`、`novel.synopsis`、`novel.status` |
| 更新世界观统计数量 | 修改 `world.json` 中的 `world.characters` / `locations` 等字段 |
| 新增衍生故事卡片 | 在 `world.json` 的 `stories` 数组中添加条目 |
| 新增插画 | 在 `world.json` 的 `gallery` 数组中添加条目 |
| 修改站名/简介 | 直接编辑 `index.html` 中对应的文本节点 |
| 调整配色 | 修改 `css/main.css` 模块 1 中的 CSS 变量值 |
| 调整阅读器排版 | 修改 `js/markup.js` 中的 `CSS` 常量 |

---

## 技术栈

- **纯原生前端**：HTML5 · CSS3 · Vanilla JavaScript（无框架、无构建工具）
- **字体**：Google Fonts（Noto Serif SC · Noto Sans SC · Share Tech Mono · Cinzel）
- **部署**：GitHub Pages（推送至 `main` 分支自动发布）

---

© 2026 · vigill357 · 保留所有权利
